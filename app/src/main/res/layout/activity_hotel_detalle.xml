<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- HEADER fijo arriba -->
    <include layout="@layout/header"/>

    <!-- Contenido con margen superior propio -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:layout_marginTop="40dp"> <!-- margen entre header y contenido -->

            <!-- Campos EditText -->
            <EditText
                android:id="@+id/etNombreHotel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Nombre"
                android:padding="14dp"
                android:background="@drawable/rounded_edittext"
                android:layout_marginBottom="25dp"/>

            <EditText
                android:id="@+id/etDireccionHotel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Dirección"
                android:padding="14dp"
                android:background="@drawable/rounded_edittext"
                android:layout_marginBottom="25dp"/>

            <EditText
                android:id="@+id/etTelefonoHotel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Teléfonos"
                android:padding="14dp"
                android:background="@drawable/rounded_edittext"
                android:layout_marginBottom="25dp"/>

            <EditText
                android:id="@+id/etAnoHotel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Año de inauguración"
                android:inputType="number"
                android:padding="14dp"
                android:background="@drawable/rounded_edittext"
                android:layout_marginBottom="25dp"/>

            <!-- Spinner categoría -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Categoría"
                android:textSize="16sp"
                android:layout_marginBottom="6dp"/>

            <Spinner
                android:id="@+id/spinnerCategoria"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="28dp"/>

            <!-- Botón Actualizar categoría ancho -->
            <Button
                android:id="@+id/btnActualizarCategoria"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Actualizar categoría"
                android:backgroundTint="@color/naranjaoscuro"
                android:textColor="@color/white"
                android:textSize="16sp"
                android:padding="14dp"/>
        </LinearLayout>
    </ScrollView>
</LinearLayout>
